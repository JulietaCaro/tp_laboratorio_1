/** \brief Recibe los dos datos ingresados y calcula la suma de estos.
 * \param Primer operando de tipo float ingresado por el usuario.
 * \param Segundo operando de tipo float ingresado por el usuario.
 * \return Retorna el resultado de la suma.
 */
int suma (float x, float y);

/** \brief Recibe los dos datos ingresados y calcula la resta de estos.
 * \param Primer operando de tipo float ingresado por el usuario.
 * \param Segundo operando de tipo float ingresado por el usuario.
 * \return Retorna el resultado de la suma.
 */
int resta (float x, float y);

/** \brief Recibe los dos datos ingresados y calcula la división de estos.
 * \param Primer operando de tipo float ingresado por el usuario.
 * \param Segundo operando de tipo float ingresado por el usuario.
 * \return Retorna el resultado de la división.
 */
float division (float x, float y);

/** \brief Recibe los dos datos ingresados y calcula la multiplicación de estos.
 * \param Primer operando de tipo float ingresado por el usuario.
 * \param Segundo operando de tipo float ingresado por el usuario.
 * \return Retorna el resultado de la multiplicación.
 */
int multiplicacion (float x, float y);

/** \brief Recibe los dos datos ingresados y calcula el factorial de estos.
 * \param Primer operando de tipo float ingresado por el usuario.
 * \return Retorna el factorial de los dos números.
 */
long int factorial (float x);

